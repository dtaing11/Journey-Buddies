specVersion: "v1beta"
serviceId: "Backend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
